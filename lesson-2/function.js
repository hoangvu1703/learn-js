/**
 * 1. Hàm
 *      - Một khối mã
 *      - Làm 1 việc cụ thể
 * 2. Loại Hàm
 *      - Built-in
 *      - Tự định nghĩa
 * 3. Tính chất
 *      - không thực thi khi định nghĩa
 *      - Sẽ thực thi khi được gọi
 *      - Có thể nhận tham số
 *      - Có thể trả về 1 giá trị
 * 4. Tạo hàm đầu tiên
 */
    // tên hàm có thể chứa từ a-z, A-Z, 0-9,-.$... không được đặt số lên đầu tiên của tên hàm
// function showDialog() {
//     // code
//     alert('xin chào')
// }

// // toán tử call ()
// showDialog()


/**
 * 1 số điều cần biết về function
 * 
 * 
 * 1.Khi function khai báo trùng tên: khi định nghĩa 2 function trùng tên thì định nghĩa sau sẽ ghi đè định nghĩa trc
 * 2.Khai báo biến trong hàm: có thể khai báo biến trong hàm, nhưng phạm vi sử dụng chỉ ở trong hàm
 * 3.Định nghĩa hàm trong hàm: cũng như khai báo biến trên hàm
 */



/**
 * Các loại function
 * 1. Declaration function
 * 2. Expression function
 * 3. Arrow function
 */

