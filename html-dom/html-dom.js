//HTML DOM là Document Object Model , là tiêu chuẩn do W3C đưa ra
// Dom có 3 thành phần

/**
 * 1. Element gồm các thẻ html, body, h1 ,...
 * 2. Attribute gồm các thẻ href, class, ...
 * 3. Text 
 * 
*/

// https://www.w3schools.com/js/js_htmldom.asp

// mỗi hình vuông trên cây là 1 Node
// Nhắc tới Node thì phỉa hiểu là đang nói tới element, attribute, text

// Khi sử dụng javascript b có thể truy cập vào HTML DOM để có thể thay đổi tất cả các element, attribute, text trong HTML
// sử dụng HTML DOM là có thể lấy ra, chỉnh sửa, có thể thêm vào, hoặc có thể xoá HTML element 

// -------------------------------------------------------------------------------------------------------------------------------------


// Javascript chỉ cung cấp bộ phương tiện để truy xuất vào HTML DOM
// Javascript chạy trên Browser | Server (NodeJS)

// API Là Application programming interface

// Browser: HTML => DOM => DOM API (WEB API)