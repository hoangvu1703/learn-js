// Promise
    // -Sync
    // -Async
    // Nỗi đau(pain)
    // Lý Thuyết, cách hoạt động
    // Thực hành, ví dụ

// Sync/ Async

//setTimeout, setInterval, fetch, 
//XMLHttprequest, đọc file
// request animation frame

// sleep
setTimeout(function(){
    console.log(1);
}, 1000);

console.log(2)